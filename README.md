This is a short demo of how to use paraview to extract data without launching the software locally. You will still need an interactive/debug node to work from, and ideally with the same number of CPU cores as the decomposed case (though it'll still run with a lower number).

Command lines to execuse are under "script"
